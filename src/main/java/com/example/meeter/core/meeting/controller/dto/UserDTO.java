package com.example.meeter.core.meeting.controller.dto;

public record UserDTO (Long id,
                       String firstName,
                       String lastName,
                       String email){
}
